# chess_tourneer
Репозиторий для оценки кода (верстки) автора.

Для проверки и оценки верстки достаточно открыть файл `index.html` в браузере или загрузить файлы на **dev-сервер**. 

## Установка зависимостей (sass, normalize.css)
```bash
npm install -g sass
npm i
```

## Сборка стилей
```bash
npm run build
```
